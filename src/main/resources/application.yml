spring:
  application:
    name: api-gateway
  config:
    import: 'optional:configserver:http://localhost:8097'
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

spring-doc:
  apid-docs:
    enabled: false
  swagger-ui:
    enabled: false